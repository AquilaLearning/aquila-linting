{
    "workspaces": [
        "packages/**"
    ],
    "scripts": {
        "test": "nx run-many --target=test --all --output-style=stream"
    },
    "devDependencies": {
        "nx": "^22.2.0"
    }
}
